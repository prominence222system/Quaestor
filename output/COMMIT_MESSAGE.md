feat: 감독 대상 HTTP 계약면의 상태 조회 엔드포인트를 추가한다

Foreman 이 대상별 특수 코드 없이 이 감시자를 읽어갈 수 있도록,
127.0.0.1:3210 에서만 열리는 로컬 제어면을 추가했다. GET /api/health
는 프로세스 생존만 답하고 관측 상태를 읽지 않으며, GET /api/status
는 003 이 만든 deriveState() 의 판정 결과를 재해석 없이 그대로
투영한다. 이 둘을 합치면 "프로세스는 살아있고 측정은 죽어있는"
상태가 다시 초록불로 보이는, 이번 라운드가 막으려던 바로 그 문제가
재발한다.

기동 실패(포트 점유 등)는 예외로 새지 않고 started:false 로만
드러나 감시 루프 본체는 계속 돈다 — HTTP 는 부가 기능이고 차단기가
계기판 때문에 죽으면 안 된다. POST /api/stop 은 계약상 선택
조항이라 의도적으로 미구현 상태(501)로 남겼다: 이 제품의 정지는
안전장치를 끄는 것이라 확인 없이 호출되면 안 되기 때문이다.

인증(Bearer)과 watch-loop.js 배선은 이어지는 Phase 에서 다룬다.

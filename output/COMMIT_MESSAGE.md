feat: 사용량 상태를 보여주는 웹 페이지 추가

지금까지 사용량을 보려면 JSON API를 직접 호출해야 했고, 브라우저로 바로
확인할 창구가 없었다. GET /에 /api/status를 소비하는 읽기 전용 HTML
페이지를 붙여 사람이 볼 수 있는 표면을 하나 더 둔다.

측정이 죽어 있을 때(allowed: null) 초록불이나 "사용 가능"을 보여주면
28일 침묵을 화면에서 재현하는 것이므로, 무지는 항상 "모름"으로만
그리고 낡은 값(stale)은 시각적으로 구분되게 했다. 판정은 서버가 이미
낸 payload 하나를 그대로 그리기만 하며(재판정 없음), 정적 파일 서빙
경로 조립을 두지 않아 경로 탈출 가능성 자체를 없앴다. 기존 인증 게이트
순서와 /api/health·/api/status 응답은 한 글자도 바꾸지 않았다.

feat: 감독 대상 HTTP 계약면을 감시 루프에 배선해 완성한다

Foreman 이 대상별 특수 코드 없이 이 감시자를 읽어갈 수 있도록
127.0.0.1:3210 GET /api/health·/api/status 계약면을 앞선 라운드에서
만들고 인증(Bearer + 상수시간 비교)까지 얹었다. 이번 라운드는 그것을
watch-loop.js 의 mainLoop() 에 배선해 실제로 동작하게 만든다.

배선의 유일한 위험은 계기판이 차단기를 죽이는 것이었다: 지금까지
watch-loop.js 는 HTTP 를 몰랐기에 HTTP 가 어떻게 고장 나도 폴링은
계속 돌았다. 그 성질을 지키기 위해 컨트롤 서버 기동을 try/catch +
결과 분기로 이중 방어하고, 기동 성패와 무관하게 폴링 루프가
무조건 진입하도록 했다. getSnapshot() 은 기동 시점 값을 캡처하지
않는 라이브 클로저로 만들어 /api/status 가 항상 최신 관측을
반영하게 했고, 새로운 파일 I/O 를 만들지 않도록 pollOnce() 가 이미
읽는 값만 재사용했다 — 3주간 조용히 죽어 있던 측정을 초록불로
보여주는 실패를 새 층에서 반복하지 않기 위해서다.

deriveDesired() 의 임계·히스테리시스, STOP.json 스키마, 수동 STOP
우선 규칙은 세 Phase 내내 한 줄도 건드리지 않았다. POST /api/stop
은 계약상 선택 조항이지만 안전장치를 확인 없이 끄는 동작이라
의도적으로 미구현 상태를 유지하며, 그 근거는 코드 주석과
PROJECT_INTENT.md 에 남겼다.
